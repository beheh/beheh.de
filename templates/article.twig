{% extends "index.twig" %}

{% block contents %}
<div class="container">
    <article>
        <header>
            {% if nicetime %}<time>{{nicetime}}</time>{% endif %}
            <h1>{{title}}</h1>
        </header>
        {{(body ~ "\n\n\[[Zurückgehen](" ~ root ~ ")\].")|markdown}}
    </article>
</div>
{% endblock %}
